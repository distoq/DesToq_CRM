const Home = ()=> {
    
    return (
        <>
            {/* if(tokenUser !== ''){
            if(decodedToken?.sub === '1'){
                history("/dashboard")
            }else{
                history("/")
            }

            } */}
        
            <div>Oi sou a home</div>
        
        </>
    )
}

export default Home